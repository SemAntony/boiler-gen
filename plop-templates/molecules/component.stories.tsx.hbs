import type { Meta, StoryObj } from '@storybook/react';
import { {{pascalCase name}} } from './{{pascalCase name}}';

const meta: Meta
<typeof {{pascalCase name}}> = {
    component: {{pascalCase name}},
    title: 'ui-kit/{{layer}}/{{pascalCase name}}',
    };

    export default meta;
    type Story = StoryObj
    <typeof {{pascalCase name}}>;

        export const Primary: Story = {
        args: {
        {{#if props}}
            // example props
        {{/if}}
        },
        };
